# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Slack Configuration (at least one required for notifications)
# Bot token is preferred - enables per-task channels and emoji reactions
SLACK_BOT_TOKEN=xoxb-your-bot-token
# Webhook URL is fallback only (no per-task channels, no reactions)
SLACK_WEBHOOK_URL=
# Default channel if task doesn't have one set
DEFAULT_SLACK_CHANNEL=
# Comma-separated user IDs to auto-invite to new channels
SLACK_INVITE_USERS=

# Worker Configuration
POLL_INTERVAL_MS=1000
