# Supabase credentials (from your Supabase project settings)
SUPABASE_URL=https://hzinvalidlnlhindttbu.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Main loop interval in seconds (default: 1)
# This is how often the worker checks if any tasks are due to run
# Set to 1 for real-time polling (checks every second)
MAIN_LOOP_INTERVAL=1

# Maximum tasks to process in parallel (default: 3)
# If multiple tasks are due at the same time:
#   - Batch 1: first 3 tasks run together (staggered)
#   - Batch 2: next 3 tasks run together (staggered)
MAX_CONCURRENT_TASKS=3

# Delay in milliseconds between starting each task in a batch (default: 200)
# This prevents hitting eBay API with all requests at the exact same moment
# 200ms = 0.2 seconds between each task start
STAGGER_DELAY=200

# Optional: Slack webhook for notifications
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
